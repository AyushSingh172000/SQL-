SELECT DNAME
FROM DEPT
WHERE DEPT.DEPTNO NOT IN(SELECT DEPTNO
FROM EMP
WHERE EMP.DEPTNO IN DEPT.DEPTNO)
/

SQL> SELECT*
  2  FROM DEPT;

    DEPTNO DNAME          LOC
---------- -------------- -------------
        10 ACCOUNTING     NEW YORK
        20 RESEARCH       DALLAS
        30 SALES          CHICAGO
        40 OPERATIONS     BOSTON

SQL> SELECT DNAME
  2  
SQL> SELECT DNAME
  2  FROM DEPT
  3  WHERE DEPT.DEPTNO IN(SELECT DEPTNO
  4  FROM EMP
  5  WHERE EMP.DEPTNO IN DEPT.DEPTNO);

DNAME
--------------
ACCOUNTING
RESEARCH
SALES

SQL> SELECT DNAME
  2  FROM DEPT
  3  WHERE DEPT.DEPTNO IN(SELECT DEPTNO
  4  FROM EMP
  5  WHERE EMP.DEPTNO NOT IN DEPT.DEPTNO);

no rows selected

SQL> ED
Wrote file afiedt.buf

  1  SELECT DNAME
  2  FROM DEPT
  3  WHERE DEPT.DEPTNO NOT IN(SELECT DEPTNO
  4  FROM EMP
  5* WHERE EMP.DEPTNO IN DEPT.DEPTNO)
SQL> /

DNAME
--------------
OPERATIONS

SQL> 